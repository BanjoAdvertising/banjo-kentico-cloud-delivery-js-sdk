
skip_tags: true

cache:
  - node_modules -> package.json

# WHYYYYYYYY
#environment:
#  NODE_ENV: development

install:
  - Install-Product node 8
  - npm install
  - npm install mocha -g
  - npm install gulp -g

build_script:
  - npm run lint

test_script:
  - npm run test